# Copy the lines below into your terminal (or a .env file that is NOT pushed to GitHub)
OPENAI_API_KEY=your-key-here
MODEL_NAME=gpt-4o-mini
TEMPERATURE=0
# Set to 1 to run without a key (uses a simple rule-based stand-in)
USE_MOCK=0
